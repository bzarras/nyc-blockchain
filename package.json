{
  "name": "nyc-blockchain",
  "version": "0.1.0",
  "description": "A utility for finding the week's blockchain events in nyc",
  "main": "src/main.ts",
  "scripts": {
    "start": "tsc && node build/main.js",
    "build": "tsc",
    "clean": "rm -r build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ben Zarras",
  "license": "MIT",
  "dependencies": {
    "@types/dotenv": "^4.0.3",
    "@types/node": "^10.1.3",
    "@types/request": "^2.47.0",
    "@types/request-promise": "^4.1.41",
    "dotenv": "^5.0.1",
    "request": "^2.87.0",
    "request-promise": "^4.2.2"
  }
}
